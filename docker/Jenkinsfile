pipeline{
    agent  any  
    stages{
        stage("Docker"){
            steps{
                echo "========Building the image========"
                sh "cd docker"
                sh "ls"
            }


        }
    }
    
}